set previewer ~/.config/lf/previewer_sandbox
set cleaner ~/.config/lf/cleaner

# Play with mpv
map m $mpv "$f" #>/dev/null 2>&1 &
# Edit with nvim
map n $nvim "$f"

#cmd ffmpeg_jp %sh ~/scripts/ffmpeg.sh "$f" & disown
#cmd ffmpeg_jp %sh setsid ~/scripts/ffmpeg.sh "$f" >/dev/null 2>&1
cmd ffmpeg_jp %sh ~/scripts/ffmpeg.sh "$f"
map o ffmpeg_jp

cmd rename_subs %sh ~/scripts/rename_subs.sh && lf -remote "send $id reload"

map <c-b> rename_subs
