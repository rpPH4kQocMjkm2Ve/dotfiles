{{ $s := output "sops" "-d" (joinPath .sourceDir "secrets.enc.yaml") | fromYaml -}}
{{ $PortProton := index $s "PortProton" -}}

[Context]
{{- if and (hasKey $PortProton .hostname) (hasKey (index $PortProton .hostname) "games_dir") }}
filesystems={{ index $PortProton .hostname "games_dir" }};!host
{{- end }}
