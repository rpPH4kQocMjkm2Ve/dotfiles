{{- $nvidia        := promptBoolOnce . "nvidia"        "NVIDIA GPU" -}}
{{- $amd_cpu       := promptBoolOnce . "amd_cpu"       "AMD CPU (for temp sensors)" -}}
{{- $laptop        := promptBoolOnce . "laptop"        "Laptop (battery, backlight, compact fonts)" -}}
{{- $tablet        := promptBoolOnce . "tablet"        "Drawing tablet (OpenTabletDriver)" -}}
{{- $ocr           := promptBoolOnce . "ocr"           "OCR (transformers_ocr)" -}}
{{- $goldendict    := promptBoolOnce . "goldendict"    "GoldenDict" -}}
{{- $subs2srs      := promptBoolOnce . "subs2srs"      "subs2srs" -}}
{{- $sparrow       := promptBoolOnce . "sparrow"       "sparrow-wallet" -}}
{{- $portproton    := promptBoolOnce . "portproton"    "PortProton" -}}
{{- $virt_manager  := promptBoolOnce . "virt_manager"  "QEMU / virt-manager" -}}

encryption = "age"
[age]
    identity = "~/.config/chezmoi/key.txt"
    recipients = [
        "age1fyc50afgtkrq7xdqr0859ncyrvncxaclvfz83qyp3zq96mg47s5quqmvxt",
	"age14r9xns8kj0kagv4k7nm8hs85v5utkcw32vwqaczwzs8cz8zrgqeqmeakz0"
    ]

[data]
    nvidia         = {{ $nvidia }}
    amd_cpu        = {{ $amd_cpu }}
    laptop         = {{ $laptop }}
    tablet         = {{ $tablet }}
    ocr            = {{ $ocr }}
    goldendict     = {{ $goldendict }}
    subs2srs       = {{ $subs2srs }}
    sparrow        = {{ $sparrow }}
    portproton     = {{ $portproton }}
    virt_manager   = {{ $virt_manager }}
